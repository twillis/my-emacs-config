;; setup for clojure development
;; (add-to-list 'load-path (concat emacs-lib-dir "/clojure-mode"))
;; (add-to-list 'load-path (concat emacs-lib-dir "/nrepl.el"))
(require 'clojure-mode)
(require 'nrepl)
