;; emacs ipython notebook
(setq load-path (cons (concat emacs-lib-dir "/emacs-websocket") load-path))
(setq load-path (cons (concat emacs-lib-dir "/emacs-ipython-notebook/lisp") load-path))
(require 'ein) 
